{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      },
      "build": {
        "env": {
          "NODE_ENV": "production"
        },
        "commands": {
          "install": "npm install",
          "build": "npm run build"
        }
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "index.html"
    }
  ]
}
